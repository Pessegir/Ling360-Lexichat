# Pin setuptools < 81: zemberek-python imports pkg_resources, which
# setuptools 81 removed — without this the app crashes on import in a fresh
# environment (e.g. Streamlit Community Cloud). Keep first so it resolves early.
setuptools<81
google-genai>=1.0.0
nltk>=3.9
editdistance>=0.8.1
pandas>=2.0
zemberek-python>=0.2.3
NlpToolkit-WordNet>=1.0.24
# Used by the OpenAI-compatible HTTP clients (Deepseek, HF, OpenRouter)
requests>=2.31
# Web app (Phase 4)
streamlit>=1.36
streamlit-autorefresh>=1.0.1
